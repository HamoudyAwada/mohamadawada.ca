import MoeSignature from "../components/MoeSignature";

export default function SignatureTest() {
  return (
    <div style={{ minHeight: "100vh", background: "#0e0e0e", display: "flex", alignItems: "center", padding: "0 48px" }}>
      <MoeSignature />
    </div>
  );
}
